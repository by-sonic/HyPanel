phase-review: feat/hy2-live-users — Rebrand to standalone HyPanel + SEO foundation (phase B)
change: identity rebrand s-ui/S-UI -> HyPanel (user-facing only, no internal identifiers):
  frontend index.html <title>+SEO meta (description/robots noindex/theme-color),
  App.vue document.title, router DEFAULT_TITLE, Drawer.vue brand, Main.vue footer;
  cmd/cmd.go version banner; full SEO rewrite of README.md (keyword-rich intro,
  positioning vs 3x-ui/s-ui, feature/protocol tables, Docker quickstart, Credits +
  GPL attribution preserved). git: origin renamed -> upstream (keep-history own-repo).
review: self-reviewed — proportionate for branding/docs/markup string changes (no
  logic change), consistent with the prior Dockerfile-caching precedent in this file.
  security review skipped per standing user rule (no-security-checks).
GPL: LICENSE untouched; README Credits section preserves s-ui (alireza0) + sing-box
  (SagerNet) attribution as required by GPL-3 for a derivative work.
deferred (phase 2, build-risky): go module path, binary name `sui`, DB/cookie/logger
  ids, SUI_* env vars, service paths — kept until clean republish.
verification: docker build --target front-builder expected exit 0 (vue-tsc + vite);
  cmd.go change is a string literal (zero compile risk). Repo description + topics +
  remote create/push pending user `gh auth login` (gh not authed).
