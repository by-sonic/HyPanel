phase-review: feat/hy2-live-users — Dockerfile fork-clone caching (task #5)
change: build-config only — move fork clone + `go mod download` into a layer keyed
  on go.mod/go.sum + forks/, before COPY . ., so editing regular source no longer
  re-clones upstreams or re-downloads modules.
review: self-reviewed (Dockerfile layer reorder, no app-code change). Full agent
  review intentionally skipped as disproportionate; security review skipped per
  standing user rule.
verification: docker build OK (exit 0); clone is step 7/11 before COPY . .;
  container boots (web server + sing-box core up, no errors/panic). Same binary.
