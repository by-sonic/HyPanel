.DS_Store
dist/
release/
backup/
bin/
db/
sui
web/html
main
tmp
.sync*
*.tar.gz

# local env files
.env.local
.env.*.local

# Log files
*.log*
.cache

# Windows build artifacts
*.exe
*.zip
s-ui-windows/
sui-*.exe
sui-*.zip
windows/sui-*.exe

# Editor directories and files
.idea
.vscode

# HyPanel forks: only forks/patched-files/ + forks/*.sh + forks/*.md are tracked.
# The upstream clones are reconstructed by forks/setup.sh on the build host.
forks/sing-box/
forks/sing-quic/
